from django.urls import path
# from .views import ShopView, CartView


appname = 'Vehicle_Spec_API'

urlpatterns = [
    # path('<int:pk>/', CartView.as_view(), name='vehicleview'),
    # path('', ShopView,as_view(), name='allitems')
]